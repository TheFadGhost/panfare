{
  "name": "panfare",
  "version": "0.1.0",
  "description": "Local-first recipe manager: exact serving scaling, unit-aware shopping lists, week planner, cook mode.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "node tools/server.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "regression": "node tools/regression.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "vitest": "^3.2.4"
  }
}
